import { RouterTransFormerDirective } from './router-trans-former.directive';

describe('RouterTransFormerDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterTransFormerDirective();
    expect(directive).toBeTruthy();
  });
});
